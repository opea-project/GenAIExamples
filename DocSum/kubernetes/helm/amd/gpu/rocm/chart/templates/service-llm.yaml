apiVersion: v1
kind: Service
metadata:
  name: docsum-llm-service
  labels:
    name: docsum-llm-service
spec:
  type: {{ .Values.llm.service.type }}
  ports:
    - port: {{ .Values.llm.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    name: docsum-llm
