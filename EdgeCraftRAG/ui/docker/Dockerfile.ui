FROM node:22.14.0 AS vue-app

RUN apt-get update -y && apt-get install -y git

WORKDIR /home/user/ui

ENV TMPFILE_PATH=/home/user/ui
ENV NODE_OPTIONS=--max-old-space-size=4096
ENV NPM_CONFIG_AUDIT=false
ENV NPM_CONFIG_FUND=false

COPY ./ui/vue/package*.json ./
RUN npm install

COPY ./ui/vue ./
RUN npm run build

FROM nginx:stable-alpine

COPY --from=vue-app /home/user/ui/dist /usr/share/nginx/html

COPY ./ui/docker/nginx.conf /etc/nginx/nginx.conf.template
COPY ./ui/docker/replace-env.sh /docker-entrypoint.d/99-render-nginx-conf.sh

RUN chmod +x /docker-entrypoint.d/99-render-nginx-conf.sh
 
EXPOSE 8082
 
CMD ["nginx", "-g", "daemon off;"]
 
